/*
package com.itis.validation.validators;


import com.taxi.dto.SignUpDto;
import com.taxi.validation.annotations.PasswordEqual;
import lombok.RequiredArgsConstructor;

import javax.validation.ConstraintValidator;
import javax.validation.ConstraintValidatorContext;

@RequiredArgsConstructor
public class PasswordEqualValidator implements ConstraintValidator<PasswordEqual, SignUpDto> {

    @Override
    public boolean isValid(SignUpDto value, ConstraintValidatorContext context) {
        return value.getPassword().equals(value.getPasswordRepeat());
    }
}
*/
